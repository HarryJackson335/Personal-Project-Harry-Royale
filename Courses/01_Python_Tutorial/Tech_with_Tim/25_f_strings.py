tim = 89
# Instead of doing: str(tim), we can insert it in a f string as follows:
x = f'hello {6 + 8} {tim} {67 + 9}' # Essentially, we put expressions inside of the curly braces in a f-string

# Printing a f-string
print(f'hello {tim}')